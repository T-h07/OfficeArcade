# OfficeArcade Shared

This area is reserved for artifacts shared between client and server.

- `contracts/` will hold shared API/data contracts.
- `types/` will hold shared type definitions and schemas.
- `docs/` will hold shared technical notes, DTO/event references, and cross-layer conventions.
