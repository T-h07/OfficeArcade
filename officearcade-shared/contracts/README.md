# Shared Contracts Placeholder

This folder will contain shared API contracts used across:

- `officearcade-server` response/request definitions
- `officearcade-client` API integration expectations

No runtime contract implementation is added in OA-PT01.
